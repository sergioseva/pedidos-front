import { PedidoLibrosPipe } from './pedido-libros.pipe';

describe('PedidoLibrosPipe', () => {
  it('create an instance', () => {
    const pipe = new PedidoLibrosPipe();
    expect(pipe).toBeTruthy();
  });
});
